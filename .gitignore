/output
/dl
/configs/knulli-*_defconfig
/.auto.deps
/.config.cmd
/.config.old
/..config.tmp
*.depend
*.o
/*.patch
/*.diff
*.orig
*.rej
*.pyc
*.DS_Store
*._.DS_Store
.config
Thumbs.db
authorized_keys
libretro-mame-tiny
/buildroot-ccache
/.ba-docker-image-available
*.code-workspace
knulli.mk
.vscode/*
make.log
package/batocera/emulationstation/batocera-emulationstation/keys.txt
package/emulationstation/knulli-emulationstation/keys.txt
.directory
*.po~
batocera-mok.key
.project
/update_server

# libretro-super core build cache: the upstream clone, ~140 core checkouts and
# the per-profile drops.  Deliberately a sibling of output/ so it survives
# "make <board>-clean" and is shared by every board on the same ABI.
/cores-cache
/emulators-cache
/armhf-cache

# convenience symlink into the batocera submodule
/python-src
