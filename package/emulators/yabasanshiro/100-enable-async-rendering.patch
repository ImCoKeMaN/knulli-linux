--- a/yabause/src/CMakeLists.txt
+++ b/yabause/src/CMakeLists.txt
@@ -35,6 +35,12 @@ set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -fomit-frame-pointer -DJSONC
 option(YAB_MULTIBUILD "Choose wether to build all ports or only a single one")
 set(YAB_PORT_BUILT FALSE)
 set(YAB_PORTS "qt" CACHE STRING "List of ports to build")
+
+# Enable async rendering support (for OSD ports and embedded systems)
+option(YAB_ASYNC_RENDERING "Enable async rendering thread for improved graphics performance" OFF)
+if(YAB_ASYNC_RENDERING)
+  add_definitions(-DYAB_ASYNC_RENDERING)
+endif()
 
 set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -fomit-frame-pointer -DJSONCPP_NO_LOCALE_SUPPORT")
 		
