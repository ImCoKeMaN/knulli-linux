#!/bin/bash

PERCENTAGE=$(knulli-battery-check capacity)

if [ ! -z "$PERCENTAGE" ]; then
  echo "Discharging: $PERCENTAGE%" >> /userdata/rgb-log.txt
  curl -d "$PERCENTAGE Discharging" -X POST localhost:1235/update-battery-state
fi

exit 0