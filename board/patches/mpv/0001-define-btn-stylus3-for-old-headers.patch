--- a/video/out/wayland_common.c
+++ b/video/out/wayland_common.c
@@ -18,6 +18,12 @@
 #include <errno.h>
 #include <limits.h>
 #include <linux/input-event-codes.h>
+
+/* BTN_STYLUS3 arrived in Linux 4.15; define it when building against older
+ * kernel headers so the tablet-tool button switch still compiles. */
+#ifndef BTN_STYLUS3
+#define BTN_STYLUS3 0x149
+#endif
 #include <poll.h>
 #include <time.h>
 #include <unistd.h>
